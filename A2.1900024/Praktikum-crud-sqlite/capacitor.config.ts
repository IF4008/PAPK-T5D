import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Praktikum-crud-sqlite',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
